name: CI
on:
  #push:
    #branches: [ main ]
  #pull_request:
    #branches: [ main ]
  workflow_dispatch:
    inputs:
      github_context:
        description: 'github_context'
        required: true
        default: ${{ toJSON(github) }}

jobs:
  build:
    runs-on: [ macos-latest ]
    steps:
    - name: test
      run: |
        echo '${{ inputs.github_context }}'

